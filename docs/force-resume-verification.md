# Force resume verification

This change verifies that closing a vocabulary sheet force-resumes narration from the captured character offset after the mobile dismissal animation completes.

Trigger: branch-push migration pass.
