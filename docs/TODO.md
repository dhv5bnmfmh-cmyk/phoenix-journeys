# Phoenix TODO

> 状态：可执行工作清单  
> 最后整理：2026-08-01  
> Story System Version：`1.0.0`  
> Dynamic Background System Version：`1.0.0`

本文件只保留尚未完成、可验收的工作项。长期方向见 `ROADMAP.md`，故事规范见 `story/README.md`，已经完成的内容移入 `CHANGELOG.md`。

## 使用规则

- `P0`：阻断稳定体验、发布或数据安全。
- `P1`：下一批应优先完成的产品与品质工作。
- `P2`：重要但不阻断当前版本。
- `Decision`：需要 Founder 决定，AI 不得自行确定。
- 每项完成后必须从本文件移除并在 `CHANGELOG.md` 记录。

## P0：开发治理

- [ ] **让仓库根 README 与当前产品状态一致**  
  验收：移除“首个 Journey / v0.1 单旅程”等过时描述；链接到 `docs/README.md`；不复制完整规则。

- [ ] **扩展全项目文档契约测试**  
  验收：除已完成的 Story System 契约外，CI 继续检查全部核心文档存在、互相引用有效、版本号一致、Journey 公开流程名称一致。

- [ ] **消除目录数量的多处手工断言**  
  验收：运行时数量始终从统一目录计算；发布证据中的固定快照只有一个明确维护点。

- [ ] **盘点旧“思考／表达”兼容字段与测试**  
  验收：列出仍在使用的数据、AI 能力与迁移依赖；未完成迁移前不删除；公开流程保持六步。

## P0：稳定体验

- [ ] **持续验证 iOS Safari 朗读回归**  
  验收：首次播放有声音；暂停、继续、语速变化、词语临时朗读和返回页面均从同一位置继续；进度不倒退。

- [ ] **验证恢复 Discovery 时不自动朗读**  
  验收：退出后重新进入只显示已保存位置和继续选择，未发生无声或未经操作的自动播放。

- [ ] **验证所有 Journey 的存储命名空间隔离**  
  验收：进度、朗读位置、回忆、盖章、奖励和 AI 反馈不会跨 Journey 串用。

- [ ] **建立移动端人工冒烟清单**  
  验收：至少覆盖 iPhone 窄屏、矮屏、键盘、简繁、降低动态效果和 PR Preview。

## P1：Story System 持续维护

- [ ] **生成可审查的全等级文学差异报告**  
  验收：除 41 篇母版相似度外，报告覆盖 410 个运行时短文的高频句、开场、结尾、共享 enrichment 与任意两篇最高相似度；任何 severe 或 medium 阻断 Preview。

- [ ] **继续减少普通 Journey 的共享高级扩展语料**  
  验收：Lv.7–10 的新增内容优先来自目的地专属人物、冲突、来源和文化机制；共享句不得承担核心剧情或形成连续模板；修改后全库矩阵与 410 检查通过。

- [ ] **建立 Story Review 审核 artifact**  
  验收：涉及故事的 PR 自动保存 `STORY_REVIEW_PROMPT.md` 产生的严重／中等／轻微问题、修改轮次、十道 Gate 与最终签核结果。

- [ ] **持续校准 Story System 矩阵**  
  验收：新增 Journey 前更新人物、情绪、剧情、文化、节奏、结局和主题分布；三项以上同构自动退回重构。

## P1：地图与护照

- [ ] **继续校准世界地图到城市的航线路径**  
  验收：起点、航线、缩放和落点与实际城市位置一致；飞机由上方进入落点；抵达后不重复飞行。

- [ ] **提升护照透明地图层次**  
  验收：城市热点可发现；Journey tile 保持紧凑透明；只突出旅程名和小盖章；不恢复左侧大型列表。

- [ ] **检查同一城市多目的地选择**  
  验收：抵达城市后只显示该城市目的地；每个目的地使用独立 Journey ID、地理节点、背景和存储。

- [ ] **继续精修特别旅程入口**  
  验收：入口具有独立视觉识别、显示钱包状态、正确处理已解锁与余额不足，不遮挡普通护照信息。

## P1：Journey 内容扩张

- [ ] **按 Story System 矩阵继续新增普通旅程**  
  验收：每个 Journey 完整读取 `story/` 全部规范，补足当前矩阵缺口，并完整接入所有页面与服务。

- [ ] **为所有目的地补齐审查后的背景库**  
  验收：每个已发布目的地具备 10 个有效离线背景位置；资源声明、目录与测试一致。

- [ ] **复核现有 Journey 的来源质量**  
  验收：每个 Journey 至少两条权威来源；每个故事 section 有 source ID；无失效、虚构或仅依赖一般编辑来源的核心事实。

- [ ] **复核高级等级内容的独特性**  
  验收：Lv.7–10 不使用可替换城市的通用填充；故事、发现、挑战和语法修复均保持目的地主题。

- [ ] **补齐真实词语例句**  
  验收：新增或修改的重点词均有真实、自然、确实包含该词的中拼越英例句；用户运行时不等待模型生成。

## P1：视觉与声音

- [ ] **建立动态背景设备性能实测基线**  
  验收：在 iPhone Safari、Android 中端机与平板记录首屏、帧率和内存；任何明显掉帧的目的地自动降级为静态帧。

- [ ] **持续审查目的地视觉真实性**  
  验收：新增或替换的目的地素材不存在错误建筑、异常人物、错误文字、奇怪符号或不明版权来源；高质量现有素材不做无意义替换。

- [ ] **继续改善自然语音选择**  
  验收：按语言区域排序自然声音；记住用户选择；无可用声音时明确回退，不显示假播放状态。

## P1：挑战与复习

- [ ] **扩充主题专属挑战素材**  
  验收：三种挑战均不落入通用 filler；四个候选答案长度相近、唯一且与当前 Journey 主题一致。

- [ ] **强化弱项复练闭环**  
  验收：完成三连挑战后可重练最弱模式；复练不重复发放奖励；总结说明训练目标和下一步。

- [ ] **将 Journey 表现接入我的单词与长期复习**  
  验收：保存词、错误类型和复练建议可以解释来源并由用户控制。

- [ ] **继续开发独立跟读训练**  
  验收：跟读保持独立导航入口，不重新塞入 Journey 流程；支持慢速、清晰、自然示范和弱句复练。

## P2：AI 与隐私

- [ ] **为 AI 记忆增加用户可见控制**  
  验收：用户可以查看、删除和关闭发送的学习摘要；默认不服务端持久化私人内容。

- [ ] **继续加强结构化输出与回退**  
  验收：所有 Agent 返回结构可验证；模型失败、格式错误或超时时不阻断 Journey。

- [ ] **为每个 Journey 维护审查后的 AI 上下文**  
  验收：Guide、Writing、Vocabulary 等专业 Agent 不回退到北京或通用假上下文。

## P2：产品化

- [ ] **实现可恢复的账户与跨设备同步设计**  
  验收：本地优先；冲突规则、离线行为、删除与迁移清晰；未设计完成前不上传私人学习记录。

- [ ] **正式接入免费与付费访问模式**  
  验收：免费 Explorer 每日稳定获得上午与下午 Journey；付费 Explorer 普通 Journey 无次数限制；开发 Preview 全开放。

- [ ] **增加订阅恢复与权限测试**  
  验收：重装、换设备、离线和支付回调失败时权限不会错误丢失或越权。

- [ ] **建立隐私友好的错误与性能观测**  
  验收：不记录正文、私人写作或密钥；仅收集必要技术指标并提供关闭或说明。

## Decision：等待 Founder 决定

- [ ] **碎银兑换规则**  
  待决定：是否可兑换铜币、银币、金币，以及比例、上限和防重复规则。

- [ ] **特别 Journey 最终价格体系**  
  待决定：不同币种专属 Journey、价格分层、是否永久解锁。

- [ ] **AI Guide / Writing 的最终入口**  
  已确定：不得恢复到六步 Journey 主流程。  
  待决定：放在独立页面、回忆页辅助入口或个人学习中心。

- [ ] **中文稳定后的下一学习语言**  
  待决定：日语、英语或其他语言的先后次序；不得提前稀释中文主产品。
