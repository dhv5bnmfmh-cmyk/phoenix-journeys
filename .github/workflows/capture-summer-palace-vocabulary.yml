name: Capture Summer Palace vocabulary validation

on:
  pull_request:
    branches:
      - main
    types:
      - synchronize

permissions:
  contents: write

jobs:
  capture-summer-palace-vocabulary:
    if: >-
      github.event.pull_request.number == 83 &&
      github.event.pull_request.head.repo.full_name == github.repository &&
      github.event.pull_request.head.ref == 'feat/two-level-journey-picker'
    runs-on: ubuntu-latest

    steps:
      - name: Check out writable feature branch
        uses: actions/checkout@v4
        with:
          ref: ${{ github.event.pull_request.head.ref }}
          fetch-depth: 0

      - name: Reapply vocabulary completion
        shell: bash
        run: |
          python3 - <<'PY'
          from pathlib import Path

          workflow = Path('.github/workflows/fix-summer-palace-vocabulary.yml').read_text()
          start_marker = "          python3 - <<'PY'\n"
          end_marker = "          PY\n\n      - name: Set up Flutter"
          start = workflow.index(start_marker) + len(start_marker)
          end = workflow.index(end_marker, start)
          source = workflow[start:end]
          source = '\n'.join(
              line[10:] if line.startswith('          ') else line
              for line in source.splitlines()
          )
          exec(compile(source, 'fix_summer_palace_vocabulary.py', 'exec'))
          PY

      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: stable
          cache: true

      - name: Install dependencies
        working-directory: app
        run: flutter pub get

      - name: Format vocabulary content
        run: dart format app/lib/data/summer_palace_journey.dart

      - name: Capture full verification output
        shell: bash
        run: |
          set +e
          node --test worker/*.test.mjs > summer-palace-vocabulary-verify.log 2>&1
          agent_status=$?
          (cd app && flutter analyze) >> summer-palace-vocabulary-verify.log 2>&1
          analyze_status=$?
          (cd app && flutter test) >> summer-palace-vocabulary-verify.log 2>&1
          test_status=$?
          printf '\nAGENT_EXIT_CODE=%s\nANALYZE_EXIT_CODE=%s\nTEST_EXIT_CODE=%s\n' \
            "$agent_status" "$analyze_status" "$test_status" >> summer-palace-vocabulary-verify.log
          cat summer-palace-vocabulary-verify.log
          exit 0

      - name: Commit vocabulary and diagnostics
        shell: bash
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git rm .github/workflows/fix-summer-palace-vocabulary.yml
          git rm .github/workflows/capture-summer-palace-vocabulary.yml
          git add -A
          git commit -m "chore: capture Summer Palace vocabulary validation"
          git push origin HEAD:${{ github.event.pull_request.head.ref }}
